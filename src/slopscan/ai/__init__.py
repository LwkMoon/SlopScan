"""Optional AI-enhanced semantic analysis layer. Never required, never
runs unless the user passes --ai (and a provider is configured) -- see
core.pipeline for the deterministic default path, and ai.pipeline for how
the two connect."""
